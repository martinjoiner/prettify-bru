meta {
  name: Array items on 1 line
  type: http
  seq: 1
}

post {
  url: {{host}}/drinks/cocktail
  body: json
  auth: inherit
}

body:json {
  {
    "bitters": ["Angostura", "{{fish}}"],
    "ice": true
  }
}

docs {
  We expect prettify-bru to reformat the `body:json` block so the array items are on separate lines
}
