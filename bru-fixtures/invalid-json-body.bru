meta {
  name: Invalid JSON body
  type: http
  seq: 9
}

post {
  url: {{host}}/banana
  body: json
  auth: inherit
}

body:json {
    "curviness": 2,
    "colour": "yellow"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  We expect prettify-bru to format the invalid body so all 3 lines have no indentation
}
